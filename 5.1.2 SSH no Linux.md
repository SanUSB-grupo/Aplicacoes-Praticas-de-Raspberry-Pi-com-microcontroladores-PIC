## 5.1.2 _SSH_ no Linux

A maioria das distribuições Linux possuem um cliente SSH já instalado. Desta forma para utilizá-lo:
 1. Abra o terminal - ```Ctrl + Alt + T```;
 2. Digite ```ssh <usuario>@<ip do RPi>```.
    Exemplo: ```ssh pi@10.0.0.5```.
